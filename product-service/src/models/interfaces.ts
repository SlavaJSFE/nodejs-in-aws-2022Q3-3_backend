export interface Product {
  count: number,
  description: string,
  id: string,
  price: number,
  title: string,
};

export interface ProductData {
  count: number,
  description: string,
  price: number,
  title: string,
}
